int multiply(int a, int b) {
    int result = 0;
    for (int i = 0; i < b; i++)
        result += a;
    return result;
}

void print(int x) {
    @(-1 as &int) += x;
}

// int factorial(int n) {
//     int result = 1;
//     for (int i = 2; i <= n; i++)
//         result = multiply(result, i);
//     return result;
// }

void main() {
    print(multiply(2, 3));
    // int x = 0;
    // x += -(x = 3);

    // while (x < 4) {
    //     @(-1 as &int) += x;
    //     // x++;
    // } continuing {
    //     x++;
    // }
}